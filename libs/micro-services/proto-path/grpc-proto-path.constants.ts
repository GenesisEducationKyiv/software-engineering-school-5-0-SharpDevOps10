const PROTO_BASE_PATH = 'libs/proto';

export const GRPC_PROTO_PATH = {
  SUBSCRIPTION: `${PROTO_BASE_PATH}/subscription.proto`,
  WEATHER: `${PROTO_BASE_PATH}/weather.proto`,
  EMAIL: `${PROTO_BASE_PATH}/email.proto`,
};
